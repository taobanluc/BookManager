<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.linhcr.poly.bookmanager">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppThemeNoActionBar">
        <activity
            android:name=".ui.DangNhapActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.GiaoDienChinhActivity" />
        <activity android:name=".ui.ThemNguoiDungActivity" />
        <activity android:name=".ui.QuanLiNguoiDungActivity" />
        <activity android:name=".ui.ThemSachActivity" />
        <activity android:name=".ui.QuanLiSachActivity" />
        <activity android:name=".ui.ThemTheLoaiActivity" />
        <activity android:name=".ui.QuanLiTheLoaiActivity"></activity>
    </application>

</manifest>